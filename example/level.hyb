env ExampleLevel as Level

entity Quadro {
  number b

  spawn(fixed x, fixed y, number a) {
    self.b = a
  }

  Update() {

  }

  destroy(fixed something) {
    
  }

  WeaponCollision() {
    
  }

  PlayerCollision() {

  }

  WallCollision(fixed wallNormalX, fixed wallNormalY) {// bad

  }
}

struct QuadroState {
  new() {
    
  }
}

fn test(Quadro m) {// which one do we refer to?
  
}

test(spawn Quadro(10f, 10f, 0))

Pewpew::uh()
let a = Math::pi