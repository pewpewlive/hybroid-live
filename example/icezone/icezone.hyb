env IcezoneEntity as Level

use Pewpew

entity Icezone {
    fixed x, y

    fixed radius = 50fx
    number duration, time = 60

    spawn(fixed x, y) {
        self.x, self.y = x, y
        SetEntityMesh(self, IZMesh, 0)
        SpawnEntity(self, 15)
        //SetEntityRadius(self, radius)
    }

    Update() {
        let entities = GetEntitiesCollidingWithDisk(x, y, radius)
        for _, v in entities {
            
        }
    }

    destroy() {}
}