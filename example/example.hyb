@Environment(Level)

use "import_test.hyb" as test

let map:map<number> = {o:1}

let one = 1
let two = 2
one += 1

two, map.o = 10, 20

fn thing(param bool, param2 text) text {
	if param {
	} else if param2 == "oooo" {
		return "a"
	} else {
		param2 = "looool"
		return "a"
	}
	param2 = "aa"
	if param2 == "000" and param {
		return "ppp"
	}
	return param2
}

